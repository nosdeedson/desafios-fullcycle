# go-lang-hello-world

# para criar o container basta roda o comando: "docker compose up" ou "docker-compose up" 

# para testar o a imagem a partir de uma imagem do DOCKERHUB: docker run --rm --name go-lang nosdeedson/go-lang-hello-world

# link da imagem no DockerHub: https://hub.docker.com/r/nosdeedson/go-lang-hello-world
