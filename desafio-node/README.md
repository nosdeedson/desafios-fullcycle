# install express
# install: npm install --save sequelize
# install: npm install --save mysql2

# para iniciar os containers basta clonar o projeto. Rodar: "npm install",
# depois" "docker compose up -d" ou "docker-compose up -d", dependendo da versão do docker-compose na pastar raiz do projeto

# PARA ACESSAR A APLICAÇÃO USE A URL: http://localhost:8080 

# NOTA IMPORTANTE: agora com Dockerize 

